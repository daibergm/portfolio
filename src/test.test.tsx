// TODO: Delete this file when start adding tests (This is only to pass the coverage)
import { testFN } from './testUtil';

describe('test', () => {
  test('test', () => {
    const val = 1;
    expect(testFN(val)).toBe(val);
  });
});
